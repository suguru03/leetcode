'use strict';

module.exports = { templete, templete2 };

/**
 * @param {number} n
 * @return {number}
 */
function templete(n) {
  return n;
}

/**
 * @param {number} n
 * @return {number}
 */
function templete2(n) {
  return Number(n);
}
