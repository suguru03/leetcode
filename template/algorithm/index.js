'use strict';

module.exports = { template, template2 };

/**
 * @param {number} n
 * @return {number}
 */
function template(n) {
  return n;
}

/**
 * @param {number} n
 * @return {number}
 */
function template2(n) {
  return Number(n);
}
